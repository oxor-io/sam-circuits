const { generateInputData, generateInputFile, signMessage, generateTree } = require("./generateInput.js");

module.exports = {
    generateTree,
    signMessage,
    generateInputData,
    generateInputFile,
};
